even if we subtract LRGnum from the first bin(s=0) of Corrfunc result, it still 
strongly affect the chi2 fitting result:

the best parameter should be near sigma=0.3, but with the first bin, the chi2 
in sigma=0.3 is a local maximum instead of a local minimum.
